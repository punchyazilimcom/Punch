{
  "name": "punch-yazilim-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Punch Yazilim - frontend asset build (opsiyonel)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
